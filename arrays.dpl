function main()
	{
	var arr = makeArray(4)
	setArray(arr, 0, 55)
	setArray(arr, 1, 65)
	setArray(arr, 2, 75)
	setArray(arr, 3, 100)
	print(getArray(arr,0))
	print(" ")
	print(getArray(arr,1))
	print(" ")
	print(getArray(arr,2))
	print(" ")
	print(getArray(arr,3))

	var arrTwo = makeArray(2)
	setArray(arrTwo, 0, "Array")
	setArray(arrTwo, 1, "Test")
	print(getArray(arrTwo, 0))
	print(" ")
	print(getArray(arrTwo, 1))
	print("nLine")
	}
main()